{
  "name": "IZ TECH web",
  "install": "echo \"Static site — no build dependencies to install.\"",
  "terminals": [
    {
      "name": "static-server",
      "command": "python3 -m http.server 8000 --bind 0.0.0.0",
      "description": "Serves the static site over HTTP at http://localhost:8000. The IZ TECH landing page (index.html) renders at the root URL."
    }
  ],
  "ports": [
    { "name": "static site", "port": 8000 }
  ]
}
